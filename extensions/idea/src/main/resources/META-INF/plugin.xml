<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>com.alibaba.open-code-review-idea</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-name

         The vendor prefix is deliberate: a third party had already taken the bare
         "Open Code Review" on JetBrains Marketplace, so uploading under that name is
         rejected. The prefix also marks this listing as the one published by the
         project itself. Only the Marketplace / Plugins listing shows this name — the
         tool window and the Tools menu action read their labels from OcrBundle, which
         still says "Open Code Review". -->
    <name>Alibaba Open Code Review</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor url="https://github.com/alibaba/open-code-review">Alibaba</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-description -->
    <description><![CDATA[
        AI Code Review — powered by the open-code-review CLI.<br/>
        The official IntelliJ plugin of the
        <a href="https://github.com/alibaba/open-code-review">alibaba/open-code-review</a>
        project, published by the project itself.<br/>
        Bring AI code review into IntelliJ IDEA: start reviews from the tool window,
        stream logs live, and act on every comment right in the editor.<br/><br/>

        <b>Features</b>
        <ul>
            <li>Three review modes: workspace changes, branch comparison, or a single commit.</li>
            <li>Files-to-review preview, with one click into the native diff view.</li>
            <li>Custom review prompt: attach an extra background hint to any review.</li>
            <li>Streaming logs: watch the CLI output live and cancel anytime.</li>
            <li>Two-way sync: comment cards in the tool window and inline comment panels in
                the editor stay in sync — apply, dismiss, or flag as false positive on either side.</li>
            <li>Dedicated empty / cancelled / failed states; failures are retryable and surface
                the real CLI error.</li>
            <li>Built-in configuration panel: edit LLM provider settings, switch models, and
                test connectivity without leaving the IDE.</li>
        </ul>

        <b>Prerequisites</b>
        <ul>
            <li>Install the ocr CLI: <code>npm i -g @alibaba-group/open-code-review</code></li>
            <li>Configure an LLM provider (endpoint, API key, model) via the CLI or the plugin's
                configuration panel.</li>
        </ul>

        Source code &amp; issue tracker:
        <a href="https://github.com/alibaba/open-code-review">github.com/alibaba/open-code-review</a>
    ]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends optional="true" config-file="jcef.xml">com.intellij.modules.jcef</depends>

    <!-- 清单文件里声明式 UI 的文案表，对应上游的 package.nls.json / package.nls.zh-cn.json。
         运行时代码的文案不走这里，走 model/HostStrings.kt（对应上游 src/shared/i18n.ts）。
         注意：action 一旦声明了 text= 属性，属性值会盖掉 bundle 里的 key，所以下面的 action
         刻意不写 text/description——文案全部由 OcrBundle 提供。 -->
    <resource-bundle>messages.OcrBundle</resource-bundle>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="Open Code Review"
                    anchor="right"
                    icon="/icons/ocrToolWindow.svg"
                    factoryClass="com.alibaba.opencodereview.idea.toolwindow.OcrToolWindowFactory"/>

        <!-- 宿主侧提示用（例如配置面板还没接入时的降级提示）。
             displayType=BALLOON 对应 VS Code 的 showInformationMessage 那种一次性气泡。 -->
        <notificationGroup id="Open Code Review"
                           displayType="BALLOON"/>
    </extensions>

    <actions>
        <action id="OpenCodeReview.OpenToolWindow"
                class="com.alibaba.opencodereview.idea.actions.OpenOcrToolWindowAction">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
